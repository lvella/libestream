- Better testing on buffered_skip().
- Increase chunk size of hc-128 to 64 bytes, in order to dilute overhead.
- Test of sum_mod_p128 with y == out.
- On uhash_<bits>_update, aviod copying string to buffer if possible.
